export default {
  users(state, users) {
    state.users = users;
  },
  userDetails(state, user) {
    state.userDetails = user;
  },

  schemaDetails(state, schema) {
    state.schemaDetails = schema;
  },
};
